# 3. Design
Project Title - cafe management
Student No - 22212051
Name - 김지은
E-mail - jieunkim1117@gmail.com

#### [Revision History]
|Revision date|Version #|Description|Author|
|-------------|---------|-----------|------|
|6/2/2026|1.00|First|김지은|


### 1. Summary
본 문서는 Cafe Management의 실제 소프트웨어가 어떻게 구현될 것인지에 대한 내부 구조와 동적 행위를 명세한 소프트웨어 설계 보고서이다. 시스템을 구성하는 클래스 간의 정적 구조, 10개의 전체 유즈케이스에 대한 시간적 실행 흐름, 통합 시스템 상태 변화, 그리고 실제 구현을 위한 기술적 환경을 정의한다.

본 시스템 설계의 핵심은 계층형 아키텍처, 비동기 통신, 그리고 데이터 무결성 보호이다. 시스템의 유지보수성과 확장성을 높이기 위해 Backend를 Controller, Service, Repository의 3계층으로 철저히 분리하여 설계하였다. 프론트엔드와 백엔드를 완전히 분리하여 클라이언트는 화면 새로고침 없이 비동기로 서버와 통신하며, 서버는 JSON 형태의 순수 데이터만 반환하는 RESTful API 구조를 선택했다. 클라이언트에서의 데이터 변조를 막기 위해, 출퇴근 시간 생성이나 관리자 권한 검증, 기존 데이터 조작 방지 등 핵심 데이터 무결성 로직은 프론트엔드가 아닌 백엔드의 Service 계층에서 수행되도록 설계했다.

### 2. Class Diagram
2-1. 
<img width="936" height="888" alt="class diagram" src="https://github.com/user-attachments/assets/3b6d8502-d4af-41b5-8e87-189a0cdca082" />

2-2.
1. Application
시스템의 진입점 역할을 하는 메인 클래스이다. Spring Boot 애플리케이션을 부트스트랩하고 웹 서버를 실행하는 역할을 수행한다.
- main(String[]): Spring Boot 애플리케이션을 시작하는 메서드
***
2. PageController
사용자가 웹 브라우저를 통해 접속할 때 화면(HTML)을 반환해주는 컨트롤러 클래스이다. 시스템 접속 시 클라이언트에게 기본 사용자 인터페이스를 제공한다.
- index(): 기본 주소로 접속 시 메인 페이지를 렌더링하여 반환하는 메서드
***
3. ApiController
프론트엔드의 HTTP 통신 요청을 수신하고 응답을 반환하는 컨트롤러 클래스이다. CafeService 클래스를 호출하여 요청받은 비즈니스 로직의 처리를 맡긴다.
- cafeService(CafeService): 비즈니스 로직을 수행하기 위해 주입받은 서비스 객체
- WriteMemo(Map, HttpSession): 새로운 전달사항 메모를 작성하는 메서드
- getMemos(Pageable): 전달받은 목록을 페이징 처리하여 조회하는 메서드
- getWorkLogs(): 전체 또는 본인의 출퇴근 기록 목록을 조회하는 메서드
- join(JoinDto): 입력받은 정보로 신규 직원을 등록하는 메서드
- logout(HttpSession): 세션을 만료시켜 로그아웃을 수행하는 메서드
- updateMember(Long, Map): 관리자가 직원의 인적 사항 및 급여 정보를 수정하는 메서드
- getAllMembers(): 등록된 모든 직원 목록을 조회하는 메서드
- insertTime(Long, Map): 누락된 직원의 출퇴근 시간을 관리자가 수동으로 등록하는 메서드
- deleteMemo(Long HttpSession): 본인이 작성한 전달사항을 삭제하는 메서드
- updateMemo(Long, Map, HttpSession): 작성된 전달사항의 내용을 수정하는 메서드 
- login(Map, HttpSession): 아이디와 비밀번호를 확인하고 세션을 생성하여 로그인을 수행하는 메서드
- endWork(Long): 시스템 시간이 맞춰 퇴근 시간을 기록하는 메서드
- getMonthlyLogs(int, int): 특정 연/월에 해당하는 출퇴근 기록을 조회하는 메서드
- startWork(HttpSession): 시스템 시간에 맞춰 출근 시간을 기록하는 메서드
***
4. CafeService
시스템의 핵심 비즈니스 로직을 수행하는 서비스 클래스이다. ApiController의 호출을 받아 요청을 처리하며, Repository 클래스들을 사용하여 DB를 관리한다.
- workLogRepository(WorkLogRepository): 출퇴근 기록 DB 접근을 위한 객체
- memberRepository(MemberRepository): 직원 정보 DB 접근을 위한 객체
- memoRepository(MemoRepository): 전달사항 DB 접근을 위한 객체
- insertMissedTime(Long, LocalDateTime, LocalDateTime): 누락된 출퇴근 기록을 보완하는 비즈니스 로직을 수행하는 메서드
- getTodayLogs(): 당일의 출퇴근 기록들을 조회하여 가공하는 메서드
- getMonthlyLogs(int, int): 통계를 위해 특정 월의 기록을 모두 조회하는 메서드
- login(String, String): 입력된 ID와 비밀번호를 DB와 비교하여 일치 여부를 검증하는 메서드
- join(JoinDto): 중복 ID를 검사한 후 신규 회원 엔티티를 생성하여 DB에 저장하는 메서드
- getMemos(Pageable): 메모 데이터를 최신순으로 페이징 처리하여 가져오는 메서드
- deleteMemo(Long, String): 현재 로그인된 아이디와 작성자가 일치하는지 권한을 확인한 후 메모를 삭제하는 메서드
- getAllMembers(): 등록된 전체 직원 목록을 DB에서 조회하는 메서드
- updateMemo(Long, String, String): 작성자 권한을 확인한 후 메모 내용을 업데이트하는 메서드
- updateMemberInfo(Long, Integer, String, String, boolean): 직원의 인사정보를 업데이트하는 메서드
- saveMemo(Member, String): 로그인된 회원의 이름과 함께 새로운 메모를 DB에 저장하는 메서드
- startWork(Member): 중복 출근 여부를 검증한 후 출근 기록을 생성하는 메서드
- endWork(Long): 기존 출근 기록을 찾아 퇴근 시간(현재 시간)을 기록하고 마감하는 메서드
***
5. WorkLogRepository
데이터베이스에서 출퇴근 기록을 관리하는 인터페이스이다. CafeService 클래스는 이 인터페이스를 사용하여 근무 데이터를 다룬다.
- findAllByOrderByStartTimeDesc(): 모든 출퇴근 기록을 출근 시간을 기준으로 최신순으로 정렬해 조회하는 메서드
- findAllByStartTimeBetweenOrderByStartTimeAsc(LocalDateTime, LocalDateTime): 지정된 기간 내의 기록을 정렬하여 조회하는 메서드
- findTopByMemberLoginIdOrderByStartTimeDesc(String): 특정 직원의 가장 최근 출퇴근 기록 1건을 조회하는 메서드
***
6. MemberRepository
데이터베이스에서 직원 엔티티를 관리하는 인터페이스이다. CafeService 클래스는 이 인터페이스를 사용하여 회원가입 및 로그인 처리를 수행한다.
- findByLoginId(String): 로그인 아이디를 조건으로 데이터베이스에서 회원 정보를 검색하여 반환하는 메서드
***
7. MemoRepository
데이터베이스에서 전달사항 엔티티를 관리하는 인터페이스이다. CafeService 클래스는 이 인터페이스를 사용하여 작성된 메모를 저장 및 조회한다.
- findAllByOrderByRegDateDesc(Pageable): 모든 메모를 등록 시간을 기준으로 최신순으로 정렬하고 페이징하여 반환하는 메서드
- deleteAllByRegDateBefore(LocalDateTime): 지정된 기준 시간 이전에 작성된 메모들을 일괄 삭제하는 메서드
***
8. Member
시스템 사용자의 계정 정보 및 인사 데이터를 데이터베이스에 매핑하여 저장하는 엔티티 클래스이다.
- active(boolean): 현재 재직 여부(퇴사 시 false)
- position(String): 직급
- regularShift(String): 정규 근무 시간대
- id(Long): DB 고유 식별자
- loginId(String): 로그인을 위해 사용하는 계정 아이디
- role(String): 시스템 내 권한 (admin, user)
- hourlyWage(Integer): 직원 시급
- name(String): 직원 이름
- password(String): 비밀번호
- isActive()/setActive(boolean): 재직 상태를 조회하고 변경하는 메서드
- getId()/setId(): 회원 id를 조회하고 변경하는 메서드
- getLoginId()/setLoginId(String): 로그인 아이디를 조회하고 변경하는 메서드
- getPassword()/setPassword(String): 비밀번호를 조회하고 변경하는 메서드
- getName()/setName(String): 직원의 이름을 조회하고 변경하는 메서드
- getRole()/setRole(String): 시스템 권한을 조회하고 변경하는 메서드
- getHourlyWage()/setHourlyWage(Integer): 직원의 시급을 조회하고 변경하는 메서드
- getPosition()/setPosition(String): 직원의 직급을 조회하고 변경하는 메서드
- getRegularShift()/setRegularShift(String): 정규 근무 시간대를 조회하고 변경하는 메서드
***
9. WorkLog
근무자의 당일 출퇴근 시간을 데이터베이스에 매핑하여 기록하는 엔티티 클래스
- id(Long): 출퇴근 기록의 식별자
- memberName(String): 해당 근무 기록의 직원 이름
- memberLoginId(String): 해당 근무 기록의 직원 아이디
- startTime(LocalDateTime): 출근 시간 정보
- endTime(LocalDateTime): 퇴근 시간 정보
- getId()/setId(Long): 기록 식별자를 조회하고 변경하는 메서드
- getMemberName()/setMemberName(String): 근무자의 이름을 조회하고 변경하는 메서드
- getMemberLoginIn()/setMemberLoginId(String): 근무자의 아이디를 조회하고 변경하는 메서드
- getStartTime()/setStartTime(LocalDateTime): 출근 시간을 조회하고 변경하는 메서드
- getEndTime()/setEndTime(LocalDateTime): 퇴근 시간을 조회하고 변경하는 메서드
***
10. Memo
근무자 간에 인수인계 할 전달사항 데이터를 데이터베이스에 매핑하여 저장하는 엔티티 클래스이다.
- content(String): 전달할 메모의 본문
- wirterLoginId(String): 해당 메모 작성자의 아이디
- id(Long): 메모의 고유 식별자
- regDate(LocalDateTime): 메모가 등록된 시간
- writerName(String): 작성자의 실제 이름
- getId()/setId(Long): 메모 식별자를 조회하고 변경하는 메서드
- getWriterName()/setWriterName(String): 작성자 이름을 조회하고 변경하는 메서드
- getWriterLoginId()/setWriterLoginId(String): 작성자 아이디를 조회하고 변경하는 메서드
- getContent()/setContent(String): 메모 내용을 조회하고 변경하는 메서드
- getRegDate()/setRegDate(LocalDateTime): 작성 시간을 조회하고 변경하는 메서드
***
11. MemoDto
전달사항 데이터를 화면과 서버 간에 전달하고 주고받을 때 사용하는 DTO 클래스이다.
- regDate(LocalDateTime): 메모가 등록된 시간
- id(Long): 메모의 식별자
- content(String): 메모의 본문
- writerName(String):메모의 작성자 이름
- getId()/setId(Long): 메모 식별자를 조회하고 변견하는 메서드
- getWriterName()/setWriterName(String): 작성자 이름을 조회하고 변경하는 메서드
- getContent()/setContent(String): 메모 내용을 조회하고 변경하는 메서드
- getRegDate()/setRegDate(LocalDateTime): 등록 시간을 조회하고 변경하는 메서드
***
12. JoinDto
신규 직원을 등록할 때 화면에서 폼에 입력한 가입 정보를 서버로 전달하는 DTO 클래스이다.
- role(String): 권한(admin/user)
- name(String): 실제 이름
- loginId(String): 로그인 아이디
- password(String): 비밀번호
- getRole()/setRoles(String): 시스템 권한을 조회하고 변경하는 메서드 
- getName()/setName(String): 가입자 이름을 조회하고 변경하는 메서드
- getLoginId()/setLoginId(String): 로그인 아이디를 조회하고 변경하는 메서드
- getPassword()/setPassword(String): 비밀번호를 조회하고 변경하는 메서드

### 3. Sequence Diagram
1. 회원가입
<img width="1002" height="692" alt="회원가입" src="https://github.com/user-attachments/assets/7bfde218-b7fa-4d55-a634-7c81f7ac9b0f" />

사용자가 회원가입을 요청하는 상황이다. ApiController의 실행 발생 구간 내에서 동기 메시지가 호출된다. 대안 흐름을 통해 MemberRepository에서 아이디 중복 여부를 판별하고, 결과에 따라 반환 메시지를 분기 처리한다.
***
2. 로그인
<img width="906" height="725" alt="로그인" src="https://github.com/user-attachments/assets/e6486f52-7046-45f0-b1e7-23d66b513e0b" />

보안 인증 과정이다. CafeService.login() 메서드는 반환받은 Optional<Member>객체를 통해 비밀번호와 활성화 상태를 검증한다. 인증 성공 시 ApiController는 내부 실행을 통해 매개변수로 받은 HttpSession에 Member를 저장하고, ResponseEntity를 반환한다.
***
3. 로그아웃
<img width="676" height="512" alt="로그아웃" src="https://github.com/user-attachments/assets/54f529a1-ef7b-4cfd-b601-d7390d8fb08f" />

로그아웃 처리 과정이다. DB 접근이 불필요하므로 ApiController에서 직접 HttpSession 객체에 invalidate() 메시지를 보내 세션을 파기한다.
***
4. 출근 등록
<img width="707" height="632" alt="출근등록" src="https://github.com/user-attachments/assets/055d60cd-6fdd-4395-bc6c-e590012fb4e8" />

ApiController.startWork(HttpSession)이 세션에서 Member 객체를 꺼내 CafeService.startWork(Member)를 호출한다. 서비스는 DB에서 최근 기록을 조회해서 중복 출근 여부를 확인하고, 서버 시간을 기준으로 출근 기록을 등록한다.
***
5. 퇴근 등록
<img width="737" height="632" alt="퇴근 등록" src="https://github.com/user-attachments/assets/9d76b551-914d-4a97-8b62-88adafa14cc2" />

퇴근 처리 흐름이다. 매개변수로 전달받은 식별자를 통해 진행 중인 WorkLog를 조회한다. 대안 흐름을 사용하여 출근 기록이 존재하지 않거나 이미 퇴근 처리된 기록일 경우 예외를 발생시켜 오류 메시지를 반환한다. 검증을 통과한 상태에서만 setEndTime을 호출하여 Repository에 현재 시간 업데이트를 지시한다
***
6. 전달사항 작성
<img width="740" height="575" alt="전달사항 작성" src="https://github.com/user-attachments/assets/03e6c104-c9b1-44de-9242-cb218ccaf5a5" />

사용자가 전달사항을 작성할 때 전달 과정이다. ApiController의 실행 발생 구간에서 세션에 보관된 작성자 정보와 입력받은 내용을 saveMemo(Member, String) 동기 메시지로 CafeService에 전달한다. 서비스 계층은 MemoRepository에 save(Memo)를 지시하고, 반환 흐름을 거쳐 클라이언트에 응답을 보낸다.
***
7. 전달사항 수정 및 삭제
<img width="948" height="695" alt="전달사항 수정 및 삭제" src="https://github.com/user-attachments/assets/e5ed23f5-c225-407e-8ec2-77b5bed95fd6" />

메모의 수정 및 삭제 요청 처리이다. 프론트엔드 계층에서 먼저 작성자 본인 여부를 검증한다. 작성자가 아닌 경우 버튼을 화면에서 숨긴다. 본인일 경우 버튼을 노출시키고 서버 측으로 Update/Delete 명령을 전송하여 작업을 수행한다.
***
8. 매장 인력 정보 관리
<img width="805" height="608" alt="매장 인력 정보 관리" src="https://github.com/user-attachments/assets/8907441e-85bf-4319-9a83-db44241e6bc1" />

관리자가 ERP 시스템에서 인사 정보를 관리한다. updateMemverInfo() 호출 시, 대상 직원의 직급이 admin이면서 비활성화인 경우를 alt 흐름으로 분류해 시스템 오작동을 차단한다.
***
9.월별 기록 및 누적 통계 조회
<img width="965" height="666" alt="월별 기록 및 누적 통계 조회" src="https://github.com/user-attachments/assets/eac93500-a6b7-4649-949e-e9f00ea02644" />

월별 통계 데이터를 조회하는 흐름이다. 서버의 부하를 최소화하기 위해 WorkLogRepository는 해당 시간의 출퇴근 리스트만 조회하여 반환 메시지로 넘긴다.
***
10. 누락된 출퇴근 기록 등록
<img width="938" height="722" alt="누락된 출퇴근 기록 등록" src="https://github.com/user-attachments/assets/4a14c1d1-1f79-4d1b-886d-160b6b0f4a83" />

누락된 출퇴근 기록을 보완하는 예외 상황 처리 흐름이다. 프론트엔드 계층에서 기존 시간의 존재 여부를 확인한다. 시간이 채워져 있다면 보완 버튼을 숨겨 임의 수정을 차단한다. 누락되어 있는 칸에서만 버튼을 노출하여 서버로 데이터 수정을 요청할 수 있다.

### 4. State Machine Diagram
<img width="617" height="891" alt="state machine 전체" src="https://github.com/user-attachments/assets/8edce0ca-0e0b-4131-b5d5-e1dd79ab708a" />
<img width="765" height="626" alt="state machine detail1" src="https://github.com/user-attachments/assets/96192472-06dd-4751-aedb-f6b9cf3bf42e" />
<img width="827" height="846" alt="state machine detail2" src="https://github.com/user-attachments/assets/38baaaa1-e098-440f-b490-cd058d4ffa27" />

이 장에서는 시스템 전체를 하나의 객체로 보고 그린 State Machine Diagram에 대해 다룬다. State는 시스템 내 객체의 행동과 이벤트에 대한 축적된 결과이다. 본 시스템은 매장 근무자와 관리자 간의 상호작용이 매우 중요한 프로그램이므로, 사용자가 프로그램을 사용하면서 볼 수 있는 화면이라고 생각하고 시스템에서 어떤 화면을 띄우고 이벤트에 의해 어떻게 전환되는지를 중심으로 다이어그램을 작성했다. 회원가입과 로그인은 프로그램을 사용하기 위한 필수 요소이므로 가장 먼저 배치했고, 세션 획득 이후에는 출퇴근 관리, 전달사항, 매장 관리 중 사용자가 어떤 기능을 선택하느냐에 따라 지원하는 기능과 상태가 달라지도록 하였다.

- 로그인: 인증되지 않은 상태에서 login 이벤트 발생 시, 내부적으로 입력된 아이디/비밀번호가 유효하고 재직 중인 회원인지 검사하는 [isValid == true] 조건을 통과해야만 createSession 동작을 수행하고 Authenticated 상태로 전환된다.
- 출근: 대기 상태에서 출근 이벤트가 발생하면 기존 출근 기록의 종료 시간이 비어있지 않은지 검사하는 [endTime == null] 조건을 통과해야만 중복 출근을 막고 ClockedIn 상태로 전환된다.
- 퇴근: [startTime != null] 조건을 검사하여 출근 기록이 없을 때 퇴근하는 오류를 차단한다.
- ERP 탭 이동: 오직 사용자의 세션이 사장님일 때, [role == ADMIN] 조건일 때만 허용되어 ERPTab 상태로 전환된다.
- ERP 내부 모달 상태: 시간이 비어있는 칸일 경우 [time == null]가드 조건을 충족해 누락 시간 보완 상태가 된다
- 전달사항 수정/삭제: MemoDetail 상태에서 saveOrDelete 이벤트는 사용자가 해당 메모의 작성자 본인인지 확인하는 [isAuthor == true] 조건을 충족하여 UI 버튼이 노출되었을 때만 발생하도록 설계하였다.

### 5. Implementation Requirements
본 시스템을 실제 환경에서 구축하고 구동하기 위해 필요한 하드웨어 및 소프트웨어 요구사항은 다음과 같다.
- 하드웨어
    서버: 최소 2코어 이상의 CPU, 4GB 이상의 RAM
    클라이언트: 카페 매장 내 배치된 POS 단말기, PC, 또는 태블릿
- 소프트웨어
    운영체제: Window 10/11, macOS, Linux
    웹 브라우저: Google Chrome, Microsoft Edge
- 개발 및 실행 환경
    백엔드: Java 17 이상, Spring Boot 3.x, Spring Data JPA
    데이터베이스: H2 Database
    웹 서버: Spring Boot 내장 Apache Tomcat


### 6. Glossary
- 실행 발생 : 시퀀스 다이어그램의 생명선 위에 그려지는 직사각형 박스로, 객체가 해당 시간 동안 활성화되어 특정 메서드를 실행 중임을 나타낸다.
- 대안 흐름 : 시퀀스 다이어그램에서 조건문을 표현하는 프레임. 조건의 참/거짓에 따라 시스템이 서로 다른 메시지를 주고받음을 나타낸다.
- DTO : 사용자가 입력한 화면의 데이터를 백엔드 서버로 안전하게 전달하기 위해 묶어놓은 전송 전용 객체이다

### 7. References
- STARUML 클래스 다이어그램 : https://docs.staruml.io/working-with-uml-diagrams/class-diagram
- STARUML 시퀀스 다이어그램 : https://docs.staruml.io/working-with-uml-diagrams/sequence-diagram
- STARUML 상태머신 다이어그램 : https://docs.staruml.io/working-with-uml-diagrams/statechart-diagram
